

class config():
    """
    Get and set bot configs.
    """

    def set_config() -> bool:
        """
        Going to need several of these. At least one per config type.
        """
        return
    
    def get_config(config_name:str) -> dict:
        """
        Get a specific configuration by name, or a sub-dict of the full configuration.
        :param config_name: the name of the configuration setting.
        :return: the single config.
        """
        config = ""
        return config
    
    def load_configs() -> dict:
        """
        Get the full configuration dict.
        :return: dict, full configuration table of the bot.
        """
        configs = ""
        return configs